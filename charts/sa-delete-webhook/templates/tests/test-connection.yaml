apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sa-delete-webhook.fullname" . }}-test-connection"
  labels:
    {{- include "sa-delete-webhook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sa-delete-webhook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
